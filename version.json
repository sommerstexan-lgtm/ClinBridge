{
  "version": "v9.5.11",
  "releaseDate": "2026-02-26",
  "summary": "Version update system, settings restore fix, backup actions & scheduling",
  "installType": "github-pages",
  "whatsNew": [
    "🔔 Automatic version update notices — ClinBridge checks once per day and shows a banner when a newer version is available",
    "📋 What's New modal — full release notes with guided update steps for browser and Home Screen users",
    "🔍 Check for Updates in Settings — manual trigger any time",
    "💾 Backup Now event action — one tap from a notification saves your full backup file instantly",
    "🔄 Backup and Sync split — 'Backup Data Now' and 'Sync to Another Device' are now two separate notification action buttons",
    "📅 Day-of-week scheduling for events — set any event to fire only on specific days (e.g. Sunday-only weekly backup)",
    "✅ Activity timing confirmation — Before/After buttons now show checkmark, solid fill, and green confirmation banner",
    "📊 Medication adherence — now excludes as-needed (PRN) meds; only scheduled medications count",
    "🫁 Condition-aware symptoms — CHF, A-Fib, Hypertension, and PPH patients get diagnosis-specific symptoms automatically",
    "🔄 Device Sync shortcut inside Add Event modal",
    "⚠️ Save as Missed button on notifications — keep events in chronological order",
    "📋 All Notes — view every note across all dates with category filtering, edit and delete"
  ],
  "fixes": [
    "Settings not restoring after data import — full deep merge now restores every saved property including fluid increment, weight unit, custom activities, and all future fields",
    "loadSettings and loadMedicineList missing from startup sequence — settings, medicines, and events now load correctly on every page load",
    "iOS double-reload after import — version key pre-set before reload prevents second hard reload race condition",
    "Import validation — now verifies file is a real ClinBridge backup before writing to storage",
    "All Notes 'View All' button disappearing when Today/History toggled — fixed",
    "Doctor's Report medication adherence inflated by PRN medications — fixed",
    "Activity timing Before/After visual confirmation improved"
  ]
}
