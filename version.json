{
  "version": "v9.10.147",
  "date": "2026-05-19",
  "notes": "Three notification bug fixes: showEventReminder now respects per-event sound=false; navigator.vibrate comment corrected; AudioContext closed-state guard added. Version skips v9.10.146 — that release was invalidated by a Service Worker cache poisoning issue where an early broken build was cached and never evicted."
}
