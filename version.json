{
  "version": "v9.10.163",
  "date": "2026-06-02",
  "notes": "Saved Meals library now correctly reloaded after data import. Previously, importing a backup wrote the correct data to localStorage but left a stale in-memory savedMeals array active — causing subsequent meal saves to silently overwrite the restored library."
}
